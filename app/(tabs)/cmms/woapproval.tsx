import CMMSPlaceholder from '@/components/CMMSPlaceholder';

export default function WOApprovalScreen() {
  return (
    <CMMSPlaceholder
      title="WO Approval Queue"
      category="Work Order Management"
      description="Review and approve pending work orders in the approval queue."
    />
  );
}
